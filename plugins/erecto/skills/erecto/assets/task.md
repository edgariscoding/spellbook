---
name: Task
about: A unit of work — bug, feature, chore, etc.
title: ""
labels: []
---
<!-- Set labels per the repo's taxonomy, if it has one.
     Title: imperative, sentence case, no quotes, no version numbers, ≤ ~12 words.
     The issue's progress log is its comments — no Progress log section here.
     No placeholders: no "TBD", no "handle edge cases", no reference to a symbol
     nothing defines. A decision that isn't made yet may stay open — say so
     explicitly, and say who makes it. -->

## Summary
<1–3 plain-language sentences: what's wrong / what we want, and why it matters.>

## Technical detail
<Specifics: file:line pointers, the mechanism, design notes, demand signals.>

## Decisions
<!-- The road not taken. The code shows what got built; only this shows what was
     refused, and why. Closed issues are read as prior art — without this, a
     future session re-proposes a rejected approach. Omit when there was no fork. -->
<- Chose <X> over <Y> — because <reason>.>

## Done when
<!-- Acceptance criteria: the user-visible behavior that must hold. Each names
     its evidence — a unit test, a manual check, a screenshot. ("Build is green"
     is a gate, not a criterion.) Write it to be checked off line by line. -->
<- <behavior that must hold> — verified by <evidence>.>

## Out of scope
<!-- What this issue deliberately does NOT do. Stops scope creep, and stops a
     later reviewer from reading an absence as an omission. -->

## Notes / links
<Related issues (#N), reference docs, blocking dependencies.>
